
1.//Shortest JavaScript Program : Empty file
type : window on console

If we type this on console, 
on global level this is pointing to the window object

window : it is global object,  created along with GEC
and along with GEC this is created

global object in case of broser known as browser

even though our file is empty, JS Engine is creates a global object.

this === window : true => at global level in case of browser

Global Space : any code we write in javascript which is not inside a function

Whenever we create variable and function in global space, these variables and function gets attach to global object i.e. window
